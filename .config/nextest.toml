[profile.default]
slow-timeout = { period = "60s", terminate-after = 1 }
# The gallery snapshot renderer has no test harness. Run it with `cargo test -p gallery --test snapshots`.
default-filter = "not binary(snapshots)"
