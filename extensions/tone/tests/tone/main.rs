//! Tone as a processor and as a tool on the live project folder.

// Clippy allows unwrap inside `#[test]` functions only, not in the helpers next to them.
#![allow(clippy::unwrap_used)]

mod project;
mod render;
mod support;
