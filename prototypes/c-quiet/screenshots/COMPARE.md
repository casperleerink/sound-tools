# Removed relative to prototype B

Sidebar

- Tool-call rows (read, edit, build, reload) with icons, timings and diff counts; now one line, history behind a click.
- Build progress bar in the conversation.
- "Waiting for the build. You can keep playing the current version." and every sentence like it.
- "Playback stopped and project restored" and "Previous version is still running" notes; the second lives in the expanded history only.
- Build error output block in the conversation.
- "no build" badge on project edits.
- Agent plan text shown while working.
- Session line with title and time, and per-message timestamps.
- Header history and collapse buttons, the collapsed sidebar and its `mod+j` shortcut.
- Lavender status dot next to "Agent".
- "Plan" toggle and the `mod+enter` hint in the composer.
- Provider name next to the model.
- Prototype state toggle and the "Primitives" link.

Transport

- Build status chip (building, failed, reloaded) with progress bar and timing.
- Audio device and sample rate; device selection moved to the project menu.
- Cycle markers on the seek strip; the strip is now a hairline.
- Separators inside the pill.

Canvas and chrome

- Wire legend.
- Zoom cluster and the percentage; zoom is trackpad and keyboard only.
- Undo, redo and "Add tool" buttons; now in the project menu.
- Dot grid.
- Second on-screen views of Polyrhythm and Mixer, and the dotted "same instance" links.
- Sampler and Curve instances and their extensions.
- Four of the six wires.
- Wire dashes and the dark outline under each wire; one weight, kind shown by opacity.
- Accent-coloured selection ring on cards.

Cards

- Tool type name and "2 views" chip in the header.
- Segmented view switcher; now quiet text tabs.
- "Open another view" button; close button only on hover.
- Accent fills inside cards (steps, slider fills, switches, active labels).

Polyrhythm

- Mute switches, ratio readouts, half-second ruler ticks and the playhead triangle in the Patterns view.
- Cycle, grid and "Repeats every" row; moved to the Voices view.
- Bordered inset box around the lanes.

Additive

- "Partials" heading and the accent readout; a muted Hz readout remains.
- Brightness slider and its modulation marker; moved to the Voice view.
- Inset box around the sliders.

Mixer

- Pan readouts under the knobs.
- "Out 1–2", LUFS readout and the DIM button on the main strip.
- "Post sends follow the channel fader and mute." caption in the Sends view.
- Green/yellow meter gradient; meters are neutral with red above -3 dBFS.
