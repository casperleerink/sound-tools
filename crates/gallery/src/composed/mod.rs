//! Composed example screens: real product surfaces built only from `sound_ui` components and
//! plain `div` layout. One file per screen, each a stateful view with fake data.

pub mod project_menu;
pub mod sidebar;
pub mod transport;
