[package]
name = "sound-app"
version.workspace = true
edition.workspace = true

[dependencies]
gpui.workspace = true
serde_json = "1"
sound-ui.workspace = true
sound-core = { path = "../core" }
sound-daw = { path = "../daw" }
sound-runtime = { path = "../runtime" }

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[[bin]]
name = "sound-app"
path = "src/main.rs"
